class Driver extends Account {

    public Driver(String name, String document){ //viene de la clase padre
        super(name, document); //llama al metodo constructor de la clase padre - atributos y metodos
    }
    
}
